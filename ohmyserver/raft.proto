syntax = "proto3";

package raft;

service Raft {
  rpc TestCall(Cmd) returns(Ack) {}
  rpc AppendEntries(AppendEntriesArg) returns(Ack) {}
}

message Ack {
    int32 ok = 1;
}

message Cmd {
    int32 sup = 1;
}

message AppendEntriesArg {
  bytes entries = 1;
}

